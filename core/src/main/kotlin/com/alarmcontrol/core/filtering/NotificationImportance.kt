package com.alarmcontrol.core.filtering

/** Framework-free ordering equivalent to Android notification importance levels. */
enum class NotificationImportance {
    MIN,
    LOW,
    DEFAULT,
    HIGH,
    MAX,
}
