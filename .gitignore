*.iml
.gradle
/.kotlin/
/local.properties
/.idea
.DS_Store
**/build/
/captures
/.managed-avd/
.externalNativeBuild
.cxx
local.properties

# ML training pipeline (build-time dev tool; not shipped). The generated assets
# under :ml/src/main/assets ARE committed — only the local toolchain is ignored.
ml/training/.venv/
ml/semantic-training/.venv*/
**/*.safetensors
**/*.gguf
**/*.task
__pycache__/
*.pyc

# Release signing material is supplied through environment variables and never committed.
*.jks
*.keystore
*.p12
*.pfx
keystore.properties
